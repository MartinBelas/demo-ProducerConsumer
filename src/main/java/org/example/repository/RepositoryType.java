package org.example.repository;

/**
 * All possible repository types.
 * The concrete repository type is configured via application.properties file.
 */
public enum RepositoryType {

    MAP, JDBC
}
